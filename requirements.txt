# Core Flask dependencies
Flask==3.0.0
Werkzeug==3.0.1
gunicorn==23.0.0

# HTTP and networking
requests==2.31.0

# Media and subtitle handling
pysrt==1.1.2
webvtt-py==0.5.1
m3u8==4.0.0

# Environment and configuration
python-dotenv==1.0.0

# Image processing (for PWA icons)
Pillow==12.0.0

# Note: OpenVPN is a SYSTEM package, not a Python package.
# It must be installed using your system's package manager:
# 
# Ubuntu/Debian:
#   sudo apt-get update
#   sudo apt-get install openvpn
#
# CentOS/RHEL:
#   sudo yum install openvpn
#
# macOS:
#   brew install openvpn
#
# PythonAnywhere: OpenVPN is NOT available (VPN features won't work)
